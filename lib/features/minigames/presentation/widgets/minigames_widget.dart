import 'package:flutter/material.dart';

class MinigamesWidget extends StatelessWidget {
  const MinigamesWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
