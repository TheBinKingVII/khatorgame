import 'package:flutter/material.dart';

class InternetcafePage extends StatelessWidget {
  const InternetcafePage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Internet Cafe Page'),
    );
  }
}
