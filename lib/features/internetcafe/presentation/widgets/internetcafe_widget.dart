import 'package:flutter/material.dart';

class InternetcafeWidget extends StatelessWidget {
  const InternetcafeWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
